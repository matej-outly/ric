<%= rug_form_for(@banner, 
	create_path: :banners_path, 
	update_path: :banner_path, 
	html: { :multipart => true }
) do |f| %>
	<%= f.picker_row :advertiser_id, RicAdvert.advertiser_model.all.order(name: :asc), :id, :name %>
	<%= f.text_input_row :name, :text_field %>
	<%= f.text_input_row :url, :text_field %>
	<%= f.picker_row :kind %>
	<%= f.dropzone_row :file %>
	<%= f.date_picker_row :valid_from %>
	<%= f.date_picker_row :valid_to %>
	<%= f.text_input_row :priority, :number_field %>
	<%= f.primary_button_row :submit %>
	<%= f.back_link_button_row %>
<% end %>