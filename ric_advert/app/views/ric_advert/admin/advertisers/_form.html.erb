<%= rug_form_for(@advertiser, url: (@advertiser.id.nil? ? advertisers_path : advertiser_path(@advertiser))) do |f| %>
	
	<%= f.text_input_row :name, :text_field %>
	<%= f.primary_button_row :submit %>

<% end %>
