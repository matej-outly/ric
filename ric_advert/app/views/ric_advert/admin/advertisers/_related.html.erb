<%= rug_menu_for(@advertiser, name: "related") do |m| %>
	
	<% if action_name == "show" %>
		<%= m.item (t('general.action.new') + " " + t("activerecord.models.#{RicAdvert.banner_model.model_name.i18n_key}")).upcase_first, new_banner_path(advertiser_id: @advertiser.id), icon: "plus", "data-no-turbolink" => true %>
	<% end %>

<% end %>