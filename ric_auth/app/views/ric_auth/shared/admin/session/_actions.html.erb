<%= rug_menu do |m| %>
	<%= m.item t("headers.ric_auth.admin_sessions.new").capitalize, ric_auth_admin.new_session_path, icon: :sign_in, active: (controller_name == "sessions") %>
	<% if RicAuth.user_model.recoverable? %>
		<%= m.item t("headers.ric_auth.admin_passwords.new").capitalize, ric_auth_admin.new_password_path, icon: :recover, active: (controller_name == "passwords") %>
	<% end %>
<% end %>