<%= rug_form_for(@partner, 
	create_path: :partners_path, 
	update_path: :partner_path
) do |f| %>
	<%= f.text_input_row :name %>
	<%= f.text_input_row :url %>
	<%= f.dropzone_row :logo %>
	<%= f.primary_button_row :submit %>
	<%= f.back_link_button_row %>
<% end %>
