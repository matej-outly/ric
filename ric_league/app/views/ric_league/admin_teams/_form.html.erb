<%= rug_form_for(@team, create_url: :teams_path, update_url: :team_path) do |f| %>
	<%= f.text_input_row :name %>
	<%= f.text_input_row :key %>
	<%= f.text_area_row :description %>
	<%= f.dropzone_row :logo %>
	<%= f.dropzone_row :photo, crop: true %>
	<%= f.crop_row :photo %>
	<%= f.primary_button_row :submit %>
<% end %>
