<%= rug_form_for(@newsletter, url: (@newsletter.id.nil? ? newsletters_path : newsletter_path(@newsletter))) do |f| %>
	<%= f.text_input_row :subject, :text_field %>
	<%= f.text_area_row :content %>
	<%= f.primary_button_row :submit %>
<% end %>
