<%= gumby_form_for(@newsletter, url: (@newsletter.id.nil? ? admin_newsletters_path : admin_newsletter_path(@newsletter))) do |f| %>
	
	<%= f.text_input_row :subject, :text_field %>
	<%= f.text_area_row :content %>
	<%= f.primary_button_row :submit %>

<% end %>
