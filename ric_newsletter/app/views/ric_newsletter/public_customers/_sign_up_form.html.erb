<%= rug_form_for(RicNewsletter.customer_model.new, url: ric_newsletter_public.newsletter_sign_up_customers_path, html: { class: "newsletter-sign-up" }) do |f| %>
	<div class="flash_canvas"></div>
	<%= f.text_input_row :email, :email_field %>
	<%= f.primary_button_row :submit, label: t("helpers.submit.register") %>
	<%= render partial: "sign_up_form_js" %>
<% end %>