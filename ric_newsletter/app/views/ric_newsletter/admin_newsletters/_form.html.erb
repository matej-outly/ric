<%= rug_form_for(@newsletter, 
	create_path: :newsletters_path, 
	update_path: :newsletter_path
) do |f| %>
	<%= f.text_input_row :subject, :text_field %>
	<%= f.text_area_row :content %>
	<%= f.primary_button_row :submit %>
	<%= f.back_link_button_row %>
<% end %>
