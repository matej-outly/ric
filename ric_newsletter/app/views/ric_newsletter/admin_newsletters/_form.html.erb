<%= rug_form_for(@newsletter, create_url: :newsletters_path, update_url: :newsletter_path) do |f| %>
	<%= f.text_input_row :subject, :text_field %>
	<%= f.text_area_row :content %>
	<%= f.primary_button_row :submit %>
<% end %>
