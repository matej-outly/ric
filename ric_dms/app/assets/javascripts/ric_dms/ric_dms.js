$(document).ready(function() {
	/*
	// Click on file/folder to select
	$filesAndFolders = $(".ric-dms-file a, .ric-dms-folder a");

	$filesAndFolders.click(function(e) {
		e.preventDefault();
		e.stopPropagation();

		$this = $(this);
		$filesAndFolders.removeClass("active");
		$this.addClass("active")
	});

	$filesAndFolders.dblclick(function() {
		window.location = this.href;
		return true;
	});

	// Click on background to unselect
	$(".ric-dms-listing").click(function(e) {
		$filesAndFolders.removeClass("active");

	});
	*/
});