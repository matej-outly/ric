<%= rug_menu_for(@calendar, options.merge(path_base: "calendars")) do |m| %>
	<%= m.new_item %>
	<%= m.edit_item %>
	<%= m.destroy_item %>
	<%= m.item t("headers.ric_calendar.events.new"), ric_calendar.new_event_path, icon: "plus" %>
<% end %>
