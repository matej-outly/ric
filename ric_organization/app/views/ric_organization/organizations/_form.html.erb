<%= rug_form_for(@organization, 
	create_path: lambda { organizations_path }, 
	update_path: lambda { |organization| organization_path(organization) }
) do |f| %>
	<%= f.text_input_row :name, :text_field %>
	<%= f.primary_button_row :submit %>
	<%= f.back_link_button_row %>
<% end %>