<%= rug_form_for(@reference, 
	create_path: :references_path, 
	update_path: :reference_path
) do |f| %>
	<%= f.text_input_row :name, :text_field, localization: (RicReference.localization == true) %>
	<%= f.text_input_row :url %>
	<%= f.dropzone_row :logo %>
	<%= f.primary_button_row :submit %>
	<%= f.back_link_button_row %>
<% end %>
