<%= rug_menu_for(nil, name: "related") do |m| %>
	
	<!-- ... -->

	<% if @product_ticker %>

		<%= m.item t("headers.ric_assortment.admin_product_tickers.show"), product_ticker_path(@product_ticker), icon: "search" %>
		
	<% end %>

<% end %>