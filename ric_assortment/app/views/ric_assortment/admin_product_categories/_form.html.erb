<%= rug_form_for(@product_category, url: (@product_category.id.nil? ? product_categories_path : product_category_path(@product_category))) do |f| %>
	<%= f.text_input_row :name, :text_field %>
	<%= f.primary_button_row :submit %>
<% end %>
