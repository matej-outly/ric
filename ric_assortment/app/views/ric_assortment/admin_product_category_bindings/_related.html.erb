<%= rug_menu_for(nil, name: "related") do |m| %>
	
	<!-- ... -->

	<% if @product %>

		<%= m.item t("headers.ric_assortment.admin_products.show"), product_path(@product), icon: "search" %>
		
	<% end %>

<% end %>