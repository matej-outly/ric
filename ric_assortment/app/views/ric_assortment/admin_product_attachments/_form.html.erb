<%= rug_form_for(@product_attachment, url: (@product_attachment.id.nil? ? product_attachments_path : product_attachment_path(@product_attachment))) do |f| %>
	<%= f.text_input_row :file, :file_field %>
	<%= f.text_input_row :title, :text_field %>
	<%= f.primary_button_row :submit %>
<% end %>
